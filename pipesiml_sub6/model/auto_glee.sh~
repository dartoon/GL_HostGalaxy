#!/bin/bash
rm -rf glee_samp
mkdir glee_samp
cd glee_samp
for i in `seq 1 48`;do
   mkdir samp${i}
   cp ../../simulation/fits/final/*-${i}.fits samp${i}
   sed "s/-1.fits/-${i}.fits/g" ../HE > HE${i}
   mv HE${i} samp${i}
   cp ../mask.fits ../mask.fits samp${i}
done
